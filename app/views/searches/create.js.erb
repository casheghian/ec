var new_markers = <%=raw @search.restaurants.to_gmaps4rails %>;
Gmaps.map.replaceMarkers(new_markers);
Gmaps.map.listen_to_markers(new_markers);
$("#search_results").html("<%= escape_javascript(render("show")) %>");

